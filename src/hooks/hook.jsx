import React from 'react';
import { StyleSheet, View } from 'react-native';

const Hook = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Hook;
