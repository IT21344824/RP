import React from 'react';
import { StyleSheet, View } from 'react-native';

const Api = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Api;
