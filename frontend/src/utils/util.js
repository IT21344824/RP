import React from 'react';
import { StyleSheet, View } from 'react-native';

const Util = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Util;
